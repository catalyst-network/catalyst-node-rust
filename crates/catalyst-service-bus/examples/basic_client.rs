use catalyst_service_bus::ServiceBusConfig;

fn main() {
    // Minimal placeholder so the example compiles with current API.
    let _cfg = ServiceBusConfig::default();
    println!("catalyst-service-bus basic_client example placeholder");
}
